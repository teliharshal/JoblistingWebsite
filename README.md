# 💼 JobListing Project

A full-stack job listing application where users can view job posts and post new job openings. This project uses **Spring Boot (Maven)** for the backend, **React.js** for the frontend, and **MongoDB** as the database.

---

## 🧰 Tech Stack

### Backend
- **Spring Boot**
- **Maven** (for dependency management)
- **MongoDB** (NoSQL database)
- **Spring Web** (for building REST APIs)

### Frontend
- **React.js**
- **Axios** (for API calls)
- **Bootstrap / Tailwind CSS** (optional styling)

---

## 🔧 Features

### ✅ Backend Features
- `@GetMapping` to:
  - Fetch all job listings
  - Search jobs by keyword (title, company, location, etc.)
- `@PostMapping` to:
  - Add a new job to the database

### ✅ Frontend Features
- View all jobs in a card/list view
- Search jobs using a search bar
- Add new jobs via a form (connects to backend using Axios)

---

## 📁 Project Structure

### Backend (Spring Boot - Maven)